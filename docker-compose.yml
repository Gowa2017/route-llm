services:
  llmrouter:
    image: ccr.ccs.tencentyun.com/st-hub/llmrouter
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/app/config.toml
      - ./data:/app/data
    restart: unless-stopped
